IF so_opltx[] IS NOT INITIAL AND gs_base-op_ltxt NOT IN so_opltx.
  CONTINUE.
ENDIF.

IF so_ckltx[] IS NOT INITIAL AND gs_base-char_ltxt NOT IN so_ckltx.
  CONTINUE.
ENDIF.

IF so_tplnr[] IS NOT INITIAL AND gs_base-tplnr NOT IN so_tplnr.
  CONTINUE.
ENDIF.