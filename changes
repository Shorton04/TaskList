"==============================================================*
"* VALIDITY CHECK — EXACTLY SAME LOGIC AS IA39
"==============================================================*

"PLKO valid-from
IF gs_base-datuv > so_keydt-low.
  CONTINUE.
ENDIF.

"PLKO valid-to
IF gs_base-datub < so_keydt-low.
  CONTINUE.
ENDIF.

"PLPO valid-from
IF gs_base-datuv_op > so_keydt-low.
  CONTINUE.
ENDIF.

"PLPO valid-to
IF gs_base-datub_op < so_keydt-low.
  CONTINUE.
ENDIF.

"PLMK valid-from
IF gs_base-gueltigab > so_keydt-low.
  CONTINUE.
ENDIF.

"PLMK valid-to
IF gs_base-valid_to_on_db < so_keydt-low.
  CONTINUE.
ENDIF.