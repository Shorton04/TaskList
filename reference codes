table
  
  " Auto-detect if we have long text data
  IF gv_show_ltxt = gc_false.
    READ TABLE gt_display WITH KEY row_kind = 'L' TRANSPORTING NO FIELDS.
    IF sy-subrc = 0.
      gv_show_ltxt = gc_true.  " We have long text rows, show the column
    ENDIF.
  ENDIF.