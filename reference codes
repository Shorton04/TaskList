*--- Make selection screen behave like toolbar buttons
IF p_showc = 'X'.
  PERFORM toggle_char.    "Call same routine used by toolbar
ENDIF.

IF p_showl = 'X'.
  PERFORM toggle_ltxt.    "Call same routine used by toolbar
ENDIF.