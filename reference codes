"===========================================================
" DEBUG : Print all SALV column names (ECC compatible)
"===========================================================

DATA(lo_columns) = lo_alv->get_columns( ).
DATA lt_cols TYPE salv_t_column.
DATA ls_col TYPE salv_s_column.
DATA lv_name TYPE lvc_fname.

lt_cols = lo_columns->get( ).

LOOP AT lt_cols INTO ls_col.
  lv_name = ls_col-columnname.
  WRITE: / lv_name.
ENDLOOP.