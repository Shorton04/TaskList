" === Determine long-text display flags ===
DATA show_op_ltxt  TYPE abap_bool.
DATA show_char_ltxt TYPE abap_bool.

CLEAR: show_op_ltxt, show_char_ltxt.

IF so_opltx IS NOT INITIAL OR gv_show_ltxt = gc_true.
  show_op_ltxt = abap_true.
ENDIF.

IF so_chltx IS NOT INITIAL OR gv_show_ltxt = gc_true.
  show_char_ltxt = abap_true.
ENDIF.