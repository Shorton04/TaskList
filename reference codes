"===========================================================
" DEBUG: Print all SALV column names (universal-compatible)
"===========================================================
DATA(lo_columns) = lo_alv->get_columns( ).
DATA(lo_col_ref) TYPE REF TO cl_salv_column.

LOOP AT lo_columns->get( ) INTO lo_col_ref.
  WRITE: / lo_col_ref->get_columnname( ).
ENDLOOP.
"===========================================================