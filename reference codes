"=== Show Char ON but this operation has no characteristics ===
IF gv_show_char = abap_true AND lv_has_char = abap_false.

  "Still display the operation row, but without CI fields
  CLEAR: ls_disp-merknr,
         ls_disp-kurzt_char,
         ls_disp-pmethode,
         ls_disp-verwmerkm,
         ls_disp-toleranzun,
         ls_disp-toleranzob,
         ls_disp-pruefeinh,
         ls_disp-stichprver,
         ls_disp-katalgart1,
         ls_disp-auswmenge1,
         ls_disp-steuerkz,
         ls_disp-ltextkz,
         ls_disp-char_ltxt,
         ls_disp-tdformat.

  DO 28 TIMES.
    ASSIGN COMPONENT |CI{ sy-index WIDTH = '02' }| OF STRUCTURE ls_disp TO FIELD-SYMBOL(<raw>).
    ASSIGN COMPONENT |CI{ sy-index WIDTH = '02' }_TXT| OF STRUCTURE ls_disp TO FIELD-SYMBOL(<txt>).
    IF <raw> IS ASSIGNED. CLEAR <raw>. ENDIF.
    IF <txt> IS ASSIGNED. CLEAR <txt>. ENDIF.
  ENDDO.

  "Append the no-char operation row
  ls_disp-row_kind = 'O'.
  APPEND ls_disp TO gt_display.

  "Skip characteristic row logic afterwards
  CONTINUE.
ENDIF.