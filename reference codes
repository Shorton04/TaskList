DATA(lo_cols) = lo_alv->get_columns( ).
DATA(lo_col)  TYPE REF TO cl_salv_column.
DATA(name)    TYPE lvc_fname.

LOOP AT lo_cols->get( ) INTO lo_col.
  name = lo_col->get_columnname( ).
  WRITE: / name.
ENDLOOP.